# Docker Deployment - Visual Guide

Simple visual walkthrough of deploying this application with Docker.

```
┌─────────────────────────────────────────────────────────────┐
│                    YOUR CURRENT MACHINE                      │
│  ┌────────────────────────────────────────────────────┐     │
│  │  esop-silverpeak-program/                          │     │
│  │  ├── backend/                                      │     │
│  │  ├── frontend/                                     │     │
│  │  ├── .env.example                                  │     │
│  │  └── docker-compose.example.yml                    │     │
│  └────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Transfer files (USB/Git/SCP)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     TARGET MACHINE                           │
│  ┌────────────────────────────────────────────────────┐     │
│  │  esop-silverpeak-program/                          │     │
│  │  ├── backend/                                      │     │
│  │  ├── frontend/                                     │     │
│  │  ├── .env  ◄──── You create this                   │     │
│  │  └── docker-compose.yml  ◄──── Rename from example │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  Run: docker-compose up -d                                  │
│                            │                                 │
│                            ▼                                 │
│  ┌────────────────────────────────────────────────────┐     │
│  │            DOCKER CREATES CONTAINERS                │     │
│  │                                                     │     │
│  │  ┌─────────────────┐      ┌─────────────────┐     │     │
│  │  │   Backend       │      │   Frontend      │     │     │
│  │  │   Container     │◄────►│   Container     │     │     │
│  │  │                 │      │                 │     │     │
│  │  │  FastAPI:8000   │      │   React:80      │     │     │
│  │  │  + Python       │      │   + nginx       │     │     │
│  │  │  + Database     │      │                 │     │     │
│  │  └─────────────────┘      └─────────────────┘     │     │
│  │         │                           │              │     │
│  │         └───────────┬───────────────┘              │     │
│  │                     │                              │     │
│  └─────────────────────┼──────────────────────────────┘     │
│                        │                                     │
│         Exposed to network via ports 8000 & 5173            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
             Access from any browser:
         http://target-machine-ip:5173
```

## What Docker Does For You

### WITHOUT Docker ❌
```
Target Machine Needs:
├── Python 3.9+ installed
├── Node.js 18+ installed
├── pip packages installed
├── npm packages installed
├── Virtual environments configured
├── Environment variables set
└── Manual process management

Problems:
- "Works on my machine" syndrome
- Dependency conflicts
- Complex setup process
- Hard to reproduce
```

### WITH Docker ✅
```
Target Machine Needs:
└── Docker installed (that's it!)

Docker Provides:
├── Python environment (automatic)
├── Node.js environment (automatic)
├── All dependencies (automatic)
├── Configuration (automatic)
├── Process management (automatic)
└── Consistent across all machines

Benefits:
- One command deployment
- Works the same everywhere
- Easy updates
- Simple to manage
```

## File Flow Diagram

```
┌──────────────────────────────────────────────────────────┐
│ STEP 1: Prepare Configuration                            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  .env.example  ────copy────►  .env                      │
│  (template)                   (add your API keys)       │
│                                                          │
└──────────────────────────────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────────┐
│ STEP 2: Start Docker                                     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  $ docker-compose up -d                                 │
│                                                          │
│  Docker reads:                                          │
│  ├── docker-compose.yml  (what to build)                │
│  ├── .env               (your settings)                 │
│  ├── backend/Dockerfile (how to build backend)          │
│  └── frontend/Dockerfile (how to build frontend)        │
│                                                          │
└──────────────────────────────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────────┐
│ STEP 3: Docker Builds and Runs                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Backend Container:                                      │
│  ┌────────────────────────────────────┐                 │
│  │ • Downloads Python image           │                 │
│  │ • Installs requirements.txt        │                 │
│  │ • Copies backend code              │                 │
│  │ • Starts FastAPI server            │                 │
│  └────────────────────────────────────┘                 │
│                                                          │
│  Frontend Container:                                     │
│  ┌────────────────────────────────────┐                 │
│  │ • Downloads Node image             │                 │
│  │ • Builds React app                 │                 │
│  │ • Sets up nginx web server         │                 │
│  │ • Serves static files              │                 │
│  └────────────────────────────────────┘                 │
│                                                          │
└──────────────────────────────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────────┐
│ STEP 4: Application Running                             │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ✅ Backend API:     http://localhost:8000              │
│  ✅ Frontend:        http://localhost:5173              │
│  ✅ Database:        Inside backend container            │
│  ✅ All connected:   Via Docker network                  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## Port Mapping Explained

```
YOUR COMPUTER                    DOCKER CONTAINER
─────────────                    ────────────────

Browser connects to:
http://localhost:5173  ──────►  nginx:80
        │                            │
        │                            ▼
   HOST PORT                   Frontend React App
(configurable)                 (inside container)


Browser/Frontend connects to:
http://localhost:8000  ──────►  FastAPI:8000
        │                            │
        │                            ▼
   HOST PORT                   Backend Python App
(configurable)                 (inside container)
                                     │
                                     ▼
                               SQLite Database
                              (stored in volume)
```

## Commands Cheat Sheet

```
┌─────────────────────────────────────────────────────────┐
│  COMMAND                    │  WHAT IT DOES             │
├─────────────────────────────────────────────────────────┤
│  docker-compose up -d       │  Start everything         │
│  docker-compose down        │  Stop everything          │
│  docker-compose ps          │  Show status              │
│  docker-compose logs -f     │  View logs (live)         │
│  docker-compose restart     │  Restart services         │
│  docker-compose up -d --build│ Rebuild and restart      │
└─────────────────────────────────────────────────────────┘
```

## Network Access Diagram

```
┌──────────────────────────────────────────────────────────────┐
│  SAME MACHINE                                                │
│  ────────────                                                │
│                                                              │
│  Browser  ──►  http://localhost:5173  ──►  Docker Frontend  │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  DIFFERENT MACHINE ON SAME NETWORK                           │
│  ─────────────────────────────────────                       │
│                                                              │
│  Another Computer ──► http://192.168.1.100:5173 ──►         │
│                       (Server's IP address)                  │
│                              │                               │
│                              ▼                               │
│                       Docker Frontend on Server             │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  INTERNET ACCESS (with reverse proxy)                        │
│  ────────────────────────────────────                        │
│                                                              │
│  Internet ──► https://myapp.company.com ──► nginx/Traefik   │
│               (domain name + SSL)           (reverse proxy)  │
│                                                   │          │
│                                                   ▼          │
│                                            Docker Containers │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## Data Persistence

```
┌─────────────────────────────────────────────────────────┐
│  DATA STORAGE WITH DOCKER VOLUMES                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Container (temporary)        Docker Volume (permanent) │
│  ────────────────────         ───────────────────────  │
│                                                         │
│  ┌──────────────┐             ┌──────────────┐         │
│  │  Backend     │             │   Database   │         │
│  │  Container   │────save────►│   Volume     │         │
│  │              │             │              │         │
│  │  esop.db ────┼────────────►│  esop.db     │         │
│  └──────────────┘             └──────────────┘         │
│        │                              │                │
│        │ container stops              │ persists!      │
│        ▼                              ▼                │
│    data lost?                     data saved!          │
│       NO!                         (in volume)          │
│                                                         │
│  Even if you stop or delete containers,                │
│  your database is safe in the Docker volume!           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## The Docker Magic

```
Traditional Deployment:          Docker Deployment:
────────────────────            ─────────────────

Your Machine:                   Your Machine:
├── Python 3.9                  └── Docker
├── Node 18.x                        │
├── pip packages                     └─► Contains EVERYTHING
├── npm packages                         ├── Python 3.9
└── Environment config                   ├── Node 18.x
    ↓                                    ├── pip packages
  Copy files                             ├── npm packages
    ↓                                    ├── Environment config
Target Machine:                          └── Application code
├── Install Python 3.9                       ↓
├── Install Node 18.x                     One file
├── Install pip packages                     ↓
├── Install npm packages                Target Machine:
├── Configure environment               └── Docker
├── Debug version conflicts                  ↓
└── Hope it works!                      Works perfectly!

Time: 2-3 hours                         Time: 10 minutes
Success Rate: 70%                       Success Rate: 99%
```

## Need Help?

📚 **Documentation:**
- [5-Minute Guide](DOCKER_5MIN_GUIDE.md) - Quick start
- [Complete Guide](DOCKER_DEPLOYMENT_GUIDE.md) - Detailed instructions
- [Setup Checklist](DOCKER_SETUP.md) - Configuration steps
- [Port Configuration](DOCKER_PORTS.md) - Customize ports

🎯 **Key Concept:** Docker = Portable, self-contained application package that runs anywhere!
