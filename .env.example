# ============================================================================
# ESOP SilverPeak Program - Environment Configuration
# ============================================================================
#
# ⚠️ SETUP INSTRUCTIONS:
#
# 1. Copy this file:
#      cp .env.example .env
#
# 2. Edit the NEW .env file (not this .example file!)
#      Windows: notepad .env
#      Linux:   nano .env
#
# 3. Fill in your actual values below (see comments for each setting)
#
# 4. DO NOT edit docker-compose.yml - it reads from this .env automatically!
#
# See CONFIGURATION_GUIDE.md for detailed help!
# ============================================================================

# ============================================
# REQUIRED: SilverPeak API Configuration
# ============================================
# These MUST be set for the application to work
# Get these credentials from your SilverPeak SD-WAN administrator

# Base URL for SilverPeak SD-WAN API
# Example: https://silverpeak.yourdomain.com/api/v1
BASE_API_URL=https://silverpeak.example.com/api/v1

# SilverPeak API authentication token
# This is the X-AUTH-TOKEN header value
API_KEY=your_silverpeak_api_token_here

# ============================================
# Security Configuration
# ============================================

# JWT Secret Key (generate a secure random string for production)
# Generate with: openssl rand -hex 32
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7

# Default admin password (change immediately after first login)
DEFAULT_ADMIN_PASSWORD=admin123

# Database Configuration
# For SQLite (default): sqlite:///./esop.db
# For PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./esop.db

# Backend Server Configuration
# Use 0.0.0.0 for Docker to allow external access
HOST=127.0.0.1
# ⚠️ Not used in Docker mode - use BACKEND_PORT below instead

# ============================================
# CORS Origins
# ============================================
# Comma-separated list of allowed frontend URLs
# ⚠️ IMPORTANT: Keep this in sync with FRONTEND_PORT below!
# 
# For single instance (most common):
#   CORS_ORIGINS=http://localhost:5173
#
# For multiple instances (advanced - e.g., dev + staging):
#   CORS_ORIGINS=http://localhost:5173,http://localhost:5174
#
# If you change FRONTEND_PORT to 3000, change this to:
#   CORS_ORIGINS=http://localhost:3000
CORS_ORIGINS=http://localhost:5173

# ============================================
# Docker Port Mapping
# ============================================
# These control which ports are exposed on your host machine
# ⚠️ IMPORTANT: Change BACKEND_PORT if you have another service on 8001!
# Common alternatives: 8080, 8888, 9000, 3001
BACKEND_PORT=8001
FRONTEND_PORT=5173
# ⚠️ If you change FRONTEND_PORT, remember to update CORS_ORIGINS above!
# ⚠️ If you change BACKEND_PORT, remember to update VITE_API_BASE_URL below!

# Frontend Configuration
# API base URL for frontend to connect to backend
# ⚠️ IMPORTANT: This must match BACKEND_PORT above!
VITE_API_BASE_URL=http://localhost:8001
# Frontend dev server port (use in docker-compose port mapping)
VITE_PORT=5173
VITE_HOST=true
