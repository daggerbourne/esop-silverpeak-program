# ============================================
# REQUIRED: SilverPeak API Configuration
# ============================================
# These MUST be set for the application to work
# Get these credentials from your SilverPeak SD-WAN administrator

# Base URL for SilverPeak SD-WAN API
# Example: https://silverpeak.yourdomain.com/api/v1
BASE_API_URL=https://silverpeak.example.com/api/v1

# SilverPeak API authentication token
# This is the X-AUTH-TOKEN header value
API_KEY=your_silverpeak_api_token_here

# ============================================
# Security Configuration
# ============================================

# JWT Secret Key (generate a secure random string for production)
# Generate with: openssl rand -hex 32
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7

# Default admin password (change immediately after first login)
DEFAULT_ADMIN_PASSWORD=admin123

# Database Configuration
# For SQLite (default): sqlite:///./esop.db
# For PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./esop.db

# Backend Server Configuration
# Use 0.0.0.0 for Docker to allow external access
HOST=127.0.0.1
PORT=8000

# CORS Origins (comma-separated list of allowed frontend URLs)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Docker Port Mapping (for docker-compose)
# These control which ports are exposed on your host machine
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Frontend Configuration
# API base URL for frontend to connect to backend
VITE_API_BASE_URL=http://localhost:8000
# Frontend dev server port (use in docker-compose port mapping)
VITE_PORT=5173
VITE_HOST=true
